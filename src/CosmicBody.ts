export class CosmicBody {
  name: string;
  color: string;
  size: number;

  constructor(name: string, color: string, size: number) {
    this.name = name;
    this.color = color;
    this.size = size;
  }
}
